# Scripts (TC API prep)

Same CSV helper as TC FE prep — lives in the Helix repo at `scripts/export-markdown-table-to-csv.py`.

See [csv-export-rules.md](../../../references/csv-export-rules.md) for in-agent export (default) and optional script invocation.
